import { ComponentSize, Radius, Spacing, TextSize } from "@workspace/ui/constants";

export interface ComponentSizeTokens {
  height: ComponentSize;
  paddingX: Spacing;
  fontSize: TextSize;
  radius: Radius;
}