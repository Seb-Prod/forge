export type ComponentTheme = {
  bgIdle: string;
  colorTextMuted:string;
  colorText:string;

  bg: string;
  bgHover: string;
  bgActive: string;
  text: string;
  textMuted: string;
  border: string;
};